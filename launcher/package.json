{
  "name": "@minecraft-manager/launcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Self-contained CLI that applies a modpack manifest to a mods directory and launches a Fabric server.",
  "bin": {
    "mm-launcher": "./src/cli.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^3.0.4"
  }
}